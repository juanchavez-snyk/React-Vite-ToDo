// Throwaway scratch file. Safe to delete.

function add(a, b) {
  return a + b
}

function greet(name) {
  return `Hello, ${name}!`
}

console.log(greet('world'), add(2, 3))

module.exports = { add, greet }
